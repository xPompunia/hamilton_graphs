# hamilton_graphs

Algorithms exercise: generates random Hamiltonian and non-Hamiltonian graphs
with a given saturation, then finds a Hamiltonian cycle (backtracking) and an
Euler path (DFS) in the adjacency matrix.

Run with `python3 main.py`.
